<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="q">
	
	<!-- 질문 게시판 최신순 10개 조회 -->
	<select id="select_list_rank" resultType="vo.QnaboardVO">
		select * from ( select rank() over(order by q_board_idx DESC) no, q.*from QNA_BOARD q)
		where no BETWEEN 1 and 10
	</select>
	
</mapper>












